#!/usr/bin/node
// a script that prints 3 lines: (like 1-multi_languages.js)
// but by using an array of string and a loop

const mulTi = ['C is fun', 'Python is cool', 'JavScript is amazing'];
let i = 0;

while (i < mulTi.length) {
  console.log(mulTi[i]);
  i++;
}
